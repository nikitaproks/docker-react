sudo: required
services:
  - docker

before_install:
  - docker build -t nikitaproks/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nikitaproks/docker-react npm run test

language: generic
